---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Subcontractors | Resilient Commercial Solutions LLC"
  description="Partner with Resilient as a subcontractor. Vetted, insured crews for facility care and project-based work."
>
  <section class="max-w-3xl mx-auto px-6 py-12">
    <h1 class="text-4xl font-extrabold mb-4">Subcontractor Opportunities</h1>
    <p class="text-slate-700">
      We partner with dependable subcontractors across NC & FL. Minimum requirements: active business entity, COI, and references.
    </p>

    <!-- Netlify form -->
    <form
      name="subcontractors"
      method="POST"
      action="/thanks-sub"
      data-netlify="true"
      netlify
      enctype="multipart/form-data"
      class="mt-8 grid gap-4 bg-white border rounded-xl p-6"
    >
      <input type="hidden" name="form-name" value="subcontractors" />
      <p class="hidden"><label>Don’t fill this out: <input name="bot-field" /></label></p>

      <div class="grid sm:grid-cols-2 gap-4">
        <label class="text-sm">
          Company Name *
          <input required name="company" class="mt-1 w-full rounded-md border px-3 py-2" />
        </label>
        <label class="text-sm">
          Contact Name *
          <input required name="contact" class="mt-1 w-full rounded-md border px-3 py-2" />
        </label>
      </div>

      <div class="grid sm:grid-cols-2 gap-4">
        <label class="text-sm">
          Email *
          <input type="email" required name="email" class="mt-1 w-full rounded-md border px-3 py-2" />
        </label>
        <label class="text-sm">
          Phone *
          <input type="tel" required name="phone" class="mt-1 w-full rounded-md border px-3 py-2" />
        </label>
      </div>

      <div class="grid sm:grid-cols-2 gap-4">
        <label class="text-sm">
          Primary Services *
          <select required name="services" class="mt-1 w-full rounded-md border px-3 py-2">
            <option value="">Select…</option>
            <option>Routine & Periodic Cleaning</option>
            <option>Deep Cleans / Turnovers</option>
            <option>High-Touch Sanitation</option>
            <option>Floor Care (strip/wax/buff)</option>
            <option>Project-Based Staffing</option>
            <option>Other</option>
          </select>
        </label>
        <label class="text-sm">
          Service Area *
          <select required name="service-area" class="mt-1 w-full rounded-md border px-3 py-2">
            <option value="">Select…</option>
            <option>Wake County, NC</option>
            <option>Hillsborough County, FL</option>
            <option>Pasco County, FL</option>
            <option>Pinellas County, FL</option>
            <option>Other (NC/FL)</option>
          </select>
        </label>
      </div>

      <label class="text-sm">
        Tell us about your team *
        <textarea required name="notes" rows="5" class="mt-1 w-full rounded-md border px-3 py-2" placeholder="Crew size, typical scopes, availability, etc."></textarea>
      </label>

      <div class="grid sm:grid-cols-2 gap-4">
        <label class="text-sm">
          Upload COI (PDF/JPG/PNG)
          <input name="coi" type="file" accept=".pdf,.jpg,.jpeg,.png" class="mt-1 block w-full text-sm" />
        </label>
        <label class="text-sm">
          Upload W-9 (optional)
          <input name="w9" type="file" accept=".pdf,.jpg,.jpeg,.png" class="mt-1 block w-full text-sm" />
        </label>
      </div>

      <button type="submit" class="mt-2 rounded-md bg-blue-600 px-5 py-3 font-semibold text-white hover:bg-blue-700">
        Submit Application
      </button>
    </form>
  </section>
</BaseLayout>
