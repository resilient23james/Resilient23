---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Contact | Resilient Commercial Solutions LLC"
  description="Get in touch for quotes, questions, or partnership opportunities."
>
  <section class="max-w-3xl mx-auto px-6 py-12" id="quote">
    <h1 class="text-4xl font-extrabold mb-6">Contact Us</h1>
    <p class="text-slate-700">Fill out the form and we’ll follow up promptly.</p>

    <!-- Netlify form -->
    <form
      name="contact"
      method="POST"
      action="/thanks"
      data-netlify="true"
      netlify
      class="mt-8 grid gap-4 bg-white border rounded-xl p-6"
    >
      <input type="hidden" name="form-name" value="contact" />
      <p class="hidden"><label>Don’t fill this out: <input name="bot-field" /></label></p>

      <label class="text-sm">
        Name *
        <input required name="name" class="mt-1 w-full rounded-md border px-3 py-2" />
      </label>

      <div class="grid sm:grid-cols-2 gap-4">
        <label class="text-sm">
          Email *
          <input required type="email" name="email" class="mt-1 w-full rounded-md border px-3 py-2" />
        </label>
        <label class="text-sm">
          Phone *
          <input required type="tel" name="phone" class="mt-1 w-full rounded-md border px-3 py-2" />
        </label>
      </div>

      <label class="text-sm">
        Message
        <textarea name="message" rows="5" class="mt-1 w-full rounded-md border px-3 py-2"></textarea>
      </label>

      <button type="submit" class="rounded-md bg-blue-600 px-5 py-3 font-semibold text-white hover:bg-blue-700">
        Send
      </button>
    </form>
  </section>
</BaseLayout>
